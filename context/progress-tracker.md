# Progress Tracker

Update this file after every meaningful implementation change.

---

## Current Phase

Building the MVP

## Current Goal

Build the custom spec-table editor incrementally, following the revised 13-step build order (per-step docs in `context/features/`). Steps 1 (reducer + static rows + add/delete/duplicate + 200-row cap), 2 (segmented value cell + pills + toolbar + gutter), 3 (review & harden Steps 1–2 — no new features), 4 (Claude-style inline pills + hard-break multiline), 5 (the "Insert field" modal shell + caret save/restore, reusing Step 4's caret-aware insert), 6 (native Shopify fields in the modal — a static list whose selection drops a real `SHOPIFY_FIELD` pill, plus click-a-pill-to-reopen-the-modal-pre-filled → Update), 7 (search / filter inside the modal — a keyboard-navigable search box over the native-field list, pure UI), 8 (fetch the shop's product metafield definitions from Admin GraphQL — shop-scoped lazy fetch with loading / empty / error states, fetch only), and 9 (render the fetched metafields as a selectable section in the modal, covered by the Step 7 search, whose selection drops a real `METAFIELD` pill — the finished "smart pill") are complete; **Step 9.5** (Save + metaobject sync spike — inserted 2026-06-17 before Step 10, proving the React → Postgres → metaobject → read-back round trip and landing the two re-homed Save-boundary follow-ups) is **complete and browser-verified end to end**; **Step 10** (mouse drag reorder with `@dnd-kit`) is **complete and browser-verified end to end** (2026-06-19); **Step 11** (keyboard reorder + accessibility — `KeyboardSensor`, focusable handle, screen-reader announcements) is now also **complete and browser-verified end to end** (2026-06-19); next is clipboard paste (Steps 12–13).

---

## Completed

- Installed Shopify app template (React Router TypeScript version)
- Database: PostgreSQL (Neon DB)
- Successfully received and stored Shopify session data in Neon DB
- Installed the app on the development store
- Confirmed the development store's shop record is now stored in Neon
- Added shop-scoped template list access through `app/models/template.server.ts`
- Added the read-only `/app/templates` screen with empty state, status filters, and template table
- Added `createTemplateForShop` and `getTemplateByIdForShop` helpers in `app/models/template.server.ts`
- Added `/app/templates/new` route with name + status form, server-side validation, and inline error UX
- Added `/app/templates/:id` placeholder route showing name, status badge, and a "Rows editor coming soon" stub (404 when not found or wrong shop)
- Consolidated the editor into a single dynamic route: merged `app.templates_.new.tsx` into `app.templates_.$id.tsx`, branching on `params.id === "new"` (blank scaffold) vs. an existing template (fetch + 404). Deleted the standalone `new` route; `/app/templates/new` now resolves to the dynamic route. `npm run build` and ESLint pass clean.
- **Editor Step 1 complete** (reducer + static rows + add/delete/duplicate + cap). See `context/features/02-editor-step1-reducer-static-rows.md`. Built:
  - `app/utils/rows.ts` — single source of truth for the row array: `MAX_TEMPLATE_ROWS` constant (read by UI now, server later), `EditorRow`/`ValuePart` types matching `data-model.md` §6–7, key helpers (`slugifyKey`, `uniqueKey`), `newRowId` (crypto.randomUUID, kept out of the reducer), `normalizeRows`, and the pure `rowsReducer` (`ADD_ROW`, `DUPLICATE_ROW`, `DELETE_ROW`, `SET_LABEL`, `SET_VALUE_TEXT`).
  - Converted the flat route `app.templates_.$id.tsx` into a folder route (`app.templates_.$id/route.tsx`) so the editor can be co-located, and added `app.templates_.$id/SpecTableEditor.tsx` — a memoized two-column Label/Value editor (Polaris web components) with per-row duplicate/delete, an `Add row` button, and a live `Rows: N / 200` counter that disables Add/Duplicate and shows an at-limit banner at the cap.
  - Cap is enforced inside the reducer (not just disabled buttons); duplicate mints a fresh `id` + unique `key`; editing a label never rewrites the row's `key`. New data rows store one `TEXT` `valuePart` so Step 2 extends it in place.
  - Step 1 is local React state only — no Save/loader/action/Postgres/metaobject yet. The `action` still returns `{ ok: false }` for non-`new` POSTs.
  - `npm run typecheck`, `npm run lint`, and `npm run build` all pass clean.

- **Editor Step 2 complete** (segmented value cell + pills + toolbar + row gutter). See `context/features/03-editor-step2-segmented-value-cell.md`. Built:
  - `app/utils/rows.ts` — extended the reducer in place: `SET_VALUE_TEXT` now takes `partIndex` (edits one TEXT segment); added `REMOVE_VALUE_PART` (drop a part, then `normalizeValueParts` merges adjacent TEXT and guarantees ≥1 TEXT), `INSERT_VALUE_PART` (append a part, ensure a trailing empty TEXT), and `ADD_SECTION`. `ADD_ROW`/`ADD_SECTION` gained an optional `afterId` (insert below the active row; append when absent) via the `insertRowAfter` helper. Added `createSectionRow`, `placeholderMetafieldPart` (keyless `METAFIELD` — Step 3 fills it). Reducer stays pure; cap re-checked inside every row-creating action; 25-case logic check passed (text/insert/remove/merge/insert-below/cap).
  - `app/routes/app.templates_.$id/SpecTableEditor.tsx` + `SpecTableEditor.module.css` — value cell renders `valueParts` inline: TEXT → borderless, content-sized native `<input>` (the token-editor inline flow Polaris fields can't express); METAFIELD/SHOPIFY_FIELD → an `<s-chip color="strong">` with a field icon (`metafields`/`product`). Toolbar above the table (Add row / Add section / Duplicate) inserts below `activeRowId` (UI state; set on focus, shown with a subdued bg + left accent), with a separate bottom Add row that appends. Each row's gutter pairs an inert `drag-handle` (reorder wired in Step 4) with a delete. Section rows render as a distinct full-width header. Cap gates all three row-creating buttons (disabled at limit, refused in the reducer). 200 stays the single `MAX_TEMPLATE_ROWS` constant.
  - **Verified end to end in the real embedded app** (Shopify Admin dev store, not just a harness): authored "Up to `[Metafield · choose field]` hours", edited segments, removed the pill (text merged to "Up to  hours"), added rows below the active row, added a distinct section row, duplicated a row below its source, deleted via the gutter, and confirmed the bottom Add row appends. `npm run typecheck`, `npm run lint`, `npm run build` all pass clean. Still local React state only — no Save/persist yet (Step 6).

- **Editor Step 3 complete** (review & harden Steps 1–2 — no new reducer actions, no new UI surfaces). See `context/features/04-editor-step3-review-harden.md`. Read every Step 1–2 surface (A–I) against `data-model.md` and `code-standards.md`; the reducer / value-part foundation is sound. Outcome:
  - **Confirmed-sound invariants (no change):** reducer is pure (ids minted by `newRowId`, never inside the reducer); the 200-cap is enforced inside `ADD_ROW`/`ADD_SECTION`/`DUPLICATE_ROW` (not just on disabled buttons) and is unbreakable from any path; `DUPLICATE_ROW` mints a fresh `id` + fresh unique `key` and deep-copies `valueParts` (a per-part spread is a full copy — parts are flat); `SET_LABEL` never rewrites `key`; the three value-part actions no-op safely on a missing id and on `SECTION_HEADER` rows; `normalizeValueParts` merges adjacent TEXT and always keeps ≥1 TEXT, pills never end up adjacent (insert always leaves a trailing TEXT), and the empty TEXT kept between two pills is an intentional caret target; memoization holds (only the edited row re-renders); `activeRowId` clears on active-row delete; `scrollTargetRef` scrolls a new row in exactly once. Polaris usage is `<s-…>` + tokens only (no legacy `@shopify/polaris` React imports, no overridden internals); scoped CSS has no hex (only `currentColor`/`transparent` + rem/ch); a11y holds (every control keyboard-reachable + SR-labelled, drag handle `aria-hidden` and not a focus trap, row activation via `onFocusCapture`). `200` is still only `MAX_TEMPLATE_ROWS` (the lone literal; the `small-200` hits are Polaris spacing tokens). Shop isolation in `template.server.ts` re-confirmed (priority #1): `shopId` in every `where`/`data`.
  - **Fixed now (comment-only, zero behavior change):** (1) corrected the misleading `slugifyKey` doc comment — it claimed "derived at creation," but UI rows are created blank so it is never called there — and recorded the owned label→key decision (below); (2) documented why the toolbar uses `<s-stack>` not `<s-button-group>`; (3) documented the two pill workarounds (`strong` tone, explicit ✕ button) so a future cleanup can't regress them to a blue/`removable` chip.
  - **Owned decision — label → key (finding #1):** keep the provisional `row_N`/`section_N` key during editing and **derive the human-readable slug from the label at Save (Step 6)** via the retained `slugifyKey`, for rows still carrying a provisional key, never rewriting once finalized. This keeps Step 3 a true behavior freeze (no new action/surface), keeps `slugifyKey` as the Step 6 tool (not dead code), and satisfies `data-model.md` §7 + the comparison-readiness invariant (keys end up human-readable, not opaque). Rejected: opaque `row_N` keys forever (would weaken the cross-product alignment invariant and waste the separate `key`).
  - **Decisions noted:** the section header already renders full-width — its grid `2.75rem 1fr` places the title in a single `1fr` track that fills everything after the gutter (finding #2, not a bug); duplicating a `SECTION_HEADER` is intended and yields a valid section copy (finding #5).
  - **Gates:** `npm run typecheck`, `npm run lint`, `npm run build` all pass clean. **Browser re-verified (Done-when #7)** in the real embedded app (dev store, `TS template`): add row (counter `0→N` accurate, the new row auto-activates and Duplicate enables); authored "Up to `[Metafield · choose field]` hours" (type → insert pill → type); edited the leading/trailing TEXT segments independently with the pill intact between them; ✕ removed the pill and the surrounding text merged to a single "Up to  hours" segment; Add section inserted below the active row and rendered **full-width** (finding #2 confirmed) and distinct from data rows; section title edited; Add row / Duplicate inserted below the active row; the duplicate **deep-copies** (editing the copy left the source unchanged); gutter delete removed the row, decremented the counter, and cleared `activeRowId` (Duplicate re-disabled); the bottom Add row appended to the end regardless of the active row. No console errors. The 200-cap blocking-all-three + banner was not exercised by hand (impractical to create 200 rows) — it is enforced in the reducer and the counter stayed accurate throughout. Editor is still local React state only (no Save), so the test rows were not persisted.
  - Not-fixed-now items are tracked under "Step 3 Follow-ups (tracked)" below, each tagged with the step that owns it.

- **Editor Step 4 complete** (Claude-style inline pills + hard-break multiline; Session 2026-06-16). See `context/features/05-editor-step4-inline-pills.md`. The per-segment `<input>`s + chip pills are replaced by **one inline `contenteditable` surface** per value cell. Built and **browser-verified end to end** in the real embedded app. Built:
  - `app/utils/rows.ts` — added `| { type: "LINE_BREAK" }` to the `ValuePart` union (data-model §7), the `isAtomicPart` predicate, and the one new reducer action **`INSERT_VALUE_PART_AT { id, partIndex, offset, part }`** — the caret-aware insert/split: it splits the TEXT at `(partIndex, offset)` and drops `part` between the halves (or splices when the target is atomic / past the end), then `normalizeValueParts`. The editor passes plain numbers, so the reducer stays pure/DOM-free. 4.4 uses it for `LINE_BREAK`; **Step 5 reuses it** to drop a picked pill at the saved caret. `INSERT_VALUE_PART` / `placeholderMetafieldPart` are now unused by the app but kept (deleted with Step 5's modal); `normalizeValueParts` needed no change (LINE_BREAK is a TEXT-merge boundary; ≥1-TEXT guarantee holds for `[TEXT, LINE_BREAK, TEXT]`).
  - `app/utils/valueParts.ts` (new, pure, unit-tested) — the **linear caret model**: the cell is a flat run of slots (1 per TEXT char, 1 per atomic part), so a caret is one integer and the DOM layer never threads `(partIndex, offset)` pairs that shift on normalize. Exposes `linearLength`, `partOffsetToLinear` / `linearToPartOffset` (caret ↔ reducer coords for insert), `planAtomicDelete` (collapsed Backspace/Delete adjacent to an atomic → which part to remove + where the caret lands; `null` for a plain char so the browser handles it), and `planSelectionDelete` (ranged delete expressed only via existing `SET_VALUE_TEXT` + `REMOVE_VALUE_PART`, no new action).
  - `app/utils/valueDom.ts` (new, framework-free DOM glue) — `renderPartsToHost` / `readPartsFromHost` (DOM ↔ parts; readback re-pads empty runs), `getSelectionLinearRange` / `setCaretLinear` (browser caret ↔ linear), `updateCaretOnState` (the live caret-on highlight), `tokenLabels`, and `syncTrailingFiller`. **Uncontrolled while typing:** the surface re-renders from `valueParts` only on structural edits (insert/delete) and restores the caret from a linear index; plain keystrokes leave the native caret alone (the effect skips when the DOM already matches state). IME-safe (no sync mid-composition); single labelled `role="textbox"` host + per-token accessible names.
  - `app/routes/app.templates_.$id/SpecTableEditor.tsx` + `.module.css` — the `ValueCell` is now the contenteditable host: TEXT → text nodes, dynamic-field parts → atomic `contenteditable="false"` **inline link tokens** (no border, no per-pill ✕ — deleted whole by Backspace/Delete), `LINE_BREAK` → `<br>`. Enter/Shift+Enter insert a hard break at the caret; paste is plain-text-at-a-collapsed-caret only (full table paste is Steps 12–13). Removed the old `<input>`/`<s-chip>`/✕ surface and the placeholder "Insert field" button (pick-then-insert: pills only ever render complete; the picker is the Step 5 modal).
  - **Verified live** (dev store, `TS template`): a seeded sample renders "Up to `[Metafield · battery_life]` hours" as one continuous line with the token in **Polaris link-blue**; hover + caret-on backgrounds show; typing before/after the token edits only the right TEXT run; Backspace just after the token removes the whole token and the text merges to "Up to  hours"; Enter on the "Features" value adds a third line and the **`Rows: 2 / 200` counter does not change**; Backspace at a line start removes the break and rejoins; no console errors. `npm run typecheck`, `lint`, `build`, and `test:run` (now **77 tests**) all pass.
  - **The three Step 3 `[Step 4]` follow-ups are resolved:** caret/focus is stable across edits (the single surface owns caret/selection); one labelled accessible surface + per-token names (no duplicate `aria-label`s); no fiddly empty TEXT segment (one continuous surface). See the 2026-06-16 session note for the two notable in-browser deviations (Polaris tokens absent from light DOM → runtime-captured link color; the contenteditable trailing-`<br>` filler).

- **Editor Step 5 complete** (the "Insert field" modal shell + caret save/restore; Session 2026-06-16). See `context/features/06-editor-step5-insert-field-modal.md`. The field picker now lives in a focus-trapped modal **outside** the contenteditable, bridged to the in-surface caret. Built and **browser-verified end to end** in the real embedded app. **No new reducer action** (reuses Step 4's `INSERT_VALUE_PART_AT`). Built:
  - `app/routes/app.templates_.$id/SpecTableEditor.tsx` — an **Insert field** toolbar `<s-button>` (icon `metafields`) disabled until a value cell has an active caret; one editor-level `<s-modal id="insert-field-modal">` with a placeholder body and `primary-action` (**Insert**) / `secondary-actions` (**Cancel**) slots. The modal is opened/closed imperatively via the **App Bridge Modal API** (`shopify.modal.show/hide`, from `useAppBridge()`); `<s-modal>` supplies the focus trap, Esc, and outside-click dismiss natively. **Caret bridge:** `ValueCell` gained an `onCaretChange(rowId, linear | null)` prop and reports its linear caret on focus + `selectionchange`; the container keeps the live caret in `activeCaretRef`, snapshots it into `savedCaretRef` when the modal opens, and on **Insert** computes `(partIndex, offset) = linearToPartOffset(row.valueParts, saved.linear)`, queues the post-insert caret (`saved.linear + 1`) in a ref-held `pendingCaretByRowRef` Map, then dispatches `INSERT_VALUE_PART_AT`. The target `ValueCell` reconcile effect consumes the queued caret to **refocus the host + restore the caret after the new pill** — the same linear-caret restore path as an internal edit (one path, not two). The saved caret is a plain `(rowId, linear)` pair, never a DOM `Range`, so it survives focus moving into the modal. **Step 5 scaffolding:** with no picker yet, Insert drops a fixed *complete* `STEP5_STUB_PILL` (`{ type: "SHOPIFY_FIELD", field: "vendor" }` → `Field · vendor`); Step 6 replaces it with the field the merchant actually selects.
  - **Sub-step 5.4 (dead-code cleanup):** removed the append-only `INSERT_VALUE_PART` action (union member + reducer case) and `placeholderMetafieldPart()` from `app/utils/rows.ts`, plus their two/one tests from `rows.test.ts` (pick-then-insert retired both — Step 4 deferred this removal to "Step 5's modal"). Test suite is now **74 tests** (was 77); `rows.ts` coverage holds.
  - **Resolved the Step 5 doc's two open questions:** (1) **`<s-modal>` is viable in this CDN build** — `commandFor`/`command` were not needed; the imperative `shopify.modal.show/hide` works, and the native focus trap + Esc + outside-click all fire. No fallback (App Bridge `shopify.modal` *is* the chosen mechanism). (2) **Focus-vs-disabled race** — resolved by a **saved-selection model** (deliberate deviation from the doc's literal "blur nulls the gate"): a value-cell blur does **not** clear `activeCaretRef` or disable the button; the gate is dropped only when a **Label/Section field gains focus**. This both avoids the disabled-flip-swallows-the-click race *and* keeps the button keyboard-reachable (you can Tab from a value cell to the button while the saved caret persists). `onDelete` also clears the gate if the deleted row held the saved caret.
  - **Verified live** (dev store, `TS template`, dev sample rows): Insert field **disabled on load**, **enabled** when a value cell has the caret, **disabled** when a Label field is focused; the modal opens with Insert/Cancel + a dimming backdrop; **Insert** dropped a `Field · vendor` pill **exactly at the saved caret** ("Up to `[Field · vendor]` `[Metafield · battery_life]` hours"), the caret landed **after** the new pill (both adjacent tokens show the caret-on state) and **focus returned to the cell**; a **mid-word Insert split** "brightness" into "bright`[Field · vendor]`ness" while the second line stayed intact (LINE_BREAK preserved); **Esc**, **Cancel**, and an **outside click** each dismissed the modal and **inserted nothing**; the `Rows: 2 / 200` counter never changed (pills are not rows). No console errors. `npm run typecheck`, `lint`, `build`, and `test:run` (74 tests) all pass. Still local React state only — no Save/persist yet.

- **Editor Step 6 complete** (native Shopify fields in the modal — static list; Session 2026-06-17). See `context/features/07-editor-step6-native-fields.md`. The Step 5 modal shell now renders a real native-field list and one modal serves both create and edit. Built and **browser-verified end to end** in the real embedded app. **One new reducer action** (`SET_VALUE_PART`); `INSERT_VALUE_PART_AT` reused for create. Built:
  - `app/utils/shopifyFields.ts` (new, pure, unit-tested) — `NATIVE_SHOPIFY_FIELDS` (the thirteen locked snake_case `field` tokens, in display order: `vendor`, `product_type`, `category`, `tags`, `total_inventory`, `available_for_sale`, `selected_options`, `weight`, `sku`, `barcode`, `price`, `compare_at_price`, `inventory_quantity`) + a `findNativeField(field)` lookup. Entry shape is `{ field, label }` only — no product-vs-variant `source` flag (the editor never resolves values; the product/variant split is the storefront resolver's job). Static, not fetched: native fields are a Shopify platform-defined schema that never varies per shop, so the constant *is* the source of truth (metafields, which do vary per shop, are fetched in Steps 8–9). Kept a separate module so Step 7's search can filter it and `SpecTableEditor.tsx` stays free of a data literal.
  - `app/utils/rows.ts` — added the **`SET_VALUE_PART { id, partIndex, part }`** reducer case (the union member already existed): an in-place one-for-one swap of one atomic slot for the edit-a-pill flow. No split, no merge, no `normalizeValueParts` (a pill→pill swap keeps part types adjacent the same), so array length, surrounding parts, and the caret are all unchanged. No-ops on a missing id / `SECTION_HEADER` / out-of-range index. Covered in `rows.test.ts` (5 cases incl. METAFIELD→SHOPIFY_FIELD conversion + the no-op guards). `data-model.md` and the `ValuePart` union are unchanged (`SHOPIFY_FIELD` already requires only `field`).
  - `app/utils/valueDom.ts` — added `partIndexOfElement(host, target)`, which mirrors `readPartsFromHost` exactly to resolve the `valueParts` index of a clicked atomic element from the live DOM (indices shift on every structural edit, so they are recomputed on demand, never stashed on the element as `data-part-index`).
  - `app/routes/app.templates_.$id/SpecTableEditor.tsx` — the modal body is now an `<s-choice-list>` of the native fields (single-select radio group, controlled via `values`); the primary button is **disabled until a field is selected**. The toolbar **Insert field** opens it in **create** mode (Insert → `INSERT_VALUE_PART_AT` at the saved caret — the `STEP5_STUB_PILL` is gone); **clicking an existing pill** (a delegated click on the value-cell host resolving the token via `partIndexOfElement`) opens the **same** modal in **edit** mode pre-filled with that pill's `field` (Update → `SET_VALUE_PART`, caret queued just after the pill via `partOffsetToLinear`). Heading (`Insert field`/`Edit field`) and primary label (`Insert`/`Update`) reflect the mode; `editTarget`, `selectedField`, and `savedCaretRef` are reset on every open and on Cancel so create and edit can't leak into each other. `onEditPart` is kept stable (the clicked cell passes the prefill field, so the container needs no `rows` dependency) — row memoization holds.
  - **Resolved the Step 6 doc's open questions:** (1) **`<s-choice-list>` + `<s-choice>` works in this CDN build** — it renders a keyboard-navigable single-select radio list, controlled `values` reflects pre-fill, and `onChange` reports the picked value; no fallback to a custom `role="option"` listbox was needed. (`variant` is not a typed prop on the React wrapper, so it is omitted — the default renders as a list.) (2) **Editing a `METAFIELD` pill** is accepted for Step 6: the native list has no metafield entry, so the modal opens unselected and Update stays disabled until a native field is picked (selecting one converts it to `SHOPIFY_FIELD`; live metafield editing is Step 9).
  - **Verified live** (dev store, `TS template`, dev sample rows): toolbar Insert field listed the thirteen fields in the locked order; **Insert disabled** until a field was selected; selecting **Vendor** + Insert dropped `Field · vendor` at the saved caret (caret after it, focus back in the cell); clicking the pill reopened the modal as **Edit field** pre-filled with **Vendor** and the button read **Update**; picking **Price** + Update swapped it to `Field · price` **in place** (surrounding text + the adjacent `Metafield · battery_life` token intact, caret after it); reopening showed **Price** pre-selected (not stale Vendor), and **Cancel** left the pill untouched; an alternating **create** open showed `Insert field` with **no stale selection** and Insert disabled; **Esc** dismissed and inserted nothing; a create insert into the multiline **Features** value landed `Field · weight` on line 2 with the `LINE_BREAK` and line 1 intact. The `Rows: 2 / 200` counter never changed; **no console errors**. `npm run typecheck`, `lint`, `build`, and `test:run` (now **85 tests**) all pass. Still local React state only — no Save/persist yet.

- **Editor Step 7 complete** (search / filter inside the modal; Session 2026-06-17). See `context/features/08-editor-step7-search-filter.md`. A keyboard-navigable search box now sits atop the Step 6 native-field list and filters it as the merchant types — a **pure UI layer**, **no reducer change**, **no API calls**. Built and **browser-verified end to end** in the real embedded app. Built:
  - `app/utils/shopifyFields.ts` — added the pure, unit-tested **`filterNativeFields(query)`**: an empty/whitespace query returns the full list in original order; otherwise a case-insensitive substring match (order preserved) against **both** the human `label` **and** the snake_case `field` token with underscores normalised to spaces, so "price" surfaces *Price* + *Compare-at price*, "type" surfaces *Product type*, and "compare at" surfaces *Compare-at price* via the token (the locked tokens are read, never rewritten). Returns a fresh array; never mutates the source. The matcher lives here (not the component) so Step 9's metafield section can reuse the same rule. Covered by 10 new tests in `shopifyFields.test.ts` (empty/whitespace identity, label match + order, multi-word fragment, case-insensitivity, token `_`→space normalisation, trim, no-match `[]`, no-mutation, fresh-array identity) — suite now **95 tests** (was 85).
  - `app/routes/app.templates_.$id/SpecTableEditor.tsx` — added `searchQuery` state and an `<s-search-field>` as the first child of the modal body (above the `<s-choice-list>`); the list now maps `filterNativeFields(searchQuery)`, and when that is empty an `<s-paragraph>` empty state (`No fields match "<query>"`) renders in its place. `searchQuery` resets to `""` on every open (create **and** edit) and on Cancel/commit, so the modal always opens full and no query leaks between opens. **Selection is independent of the filter** (the locked decision): typing never touches `selectedField`, so a selected field filtered out of view stays selected and the primary button stays enabled/committable — verified live (select Vendor → type "price" → Insert stays enabled → clear → Vendor still checked). `onInput` (fires per keystroke, before `onChange`) drives the filter.
  - **Resolved the Step 7 doc's open questions:** (1) **`<s-search-field>` works in this CDN build** — it renders a labelled, controllable search box; `value` reflects state and `onInput` reports each keystroke (read via `event.currentTarget.value`). `labelAccessibilityVisibility="exclusive"` hides the label visually while keeping it for SR. No fallback to a plain `<input type="search">` was needed. (2) **`<s-choice-list>` re-renders correctly when its `<s-choice>` children are filtered** — the controlled `values={[selectedField]}` still reflects/commits even when the selected value is not currently rendered (the keep-selection round-trip works). (3) **Auto-focus must be deferred past the modal's open animation** — see the build-notes deviation below.
  - **Verified live** (dev store, `TS template`, dev sample rows): modal opens with the search box + full 13-field list; the search box is **auto-focused** (type immediately); "pri" → *Price* + *Compare-at price*, "ven" → *Vendor* + *Total inventory* + *Inventory quantity* (all contain "ven"), "weight" → *Weight*, "compare at" → *Compare-at price* (token normalisation); "zzz" → empty state with Insert disabled; **keep-selection** confirmed (Vendor stays selected + Insert enabled while filtered out, re-checks on clear, commits Vendor); Insert drops the pill at the saved caret (product_type / vendor / weight all landed correctly, caret after, focus back in the cell); **edit mode** opens with heading "Edit field", the search box, the full list (query reset), the clicked pill's field **pre-selected and visible**, and the button reads "Update"; **Cancel** leaves the pill untouched; the `Rows: 2 / 200` counter never changed; **no console errors**. `npm run typecheck`, `lint`, `build`, and `test:run` (95 tests) all pass. Still local React state only — no Save/persist yet.

- **Editor Step 8 complete** (fetch the shop's product metafield definitions; Session 2026-06-17). See `context/features/09-editor-step8-fetch-metafield-definitions.md`. The editor's **first real Shopify call** — a shop-scoped Admin GraphQL fetch of the shop's product metafield definitions, lazily on the first modal open, with explicit loading / empty / error states. **Fetch only — not rendered as selectable choices (that is Step 9). No reducer change, no new pill, no persistence.** Built and **browser-verified end to end** in the real embedded app. Built:
  - `app/shopify/metafieldDefinitions.server.ts` (new `app/shopify/` directory — "All Shopify API calls" per `code-standards.md` File Organization). Exports the `MetafieldDefinitionSummary` shape (`{ id, namespace, key, name, type }` — `namespace`/`key` are the locked METAFIELD pill contract, `name` is the picker label, `type` feeds a future icon/hint), the **pure `mapDefinitionsResponse(json: unknown)`** edge→summary flatten + `unknown`-narrowing, and **`fetchProductMetafieldDefinitions(admin: AdminApiContext)`** which runs the validated query (`ownerType: PRODUCT`) and **pages** through `pageInfo.hasNextPage`/`endCursor` (`first: 250`, MAX_PAGES = 10 safety cap; on cap-hit it `console.warn`s what was dropped rather than silently truncating). **Shop isolation is structural** — `authenticate.admin(request)` binds the GraphQL client to the current shop's Admin token, so the query can only return this shop's definitions (priority #1, enforced by the session, not a Prisma `where`). The GraphQL query was **validated with `validate_graphql_codeblocks`** against the app's API version (2025-10 / `ApiVersion.October25`). **Malformed-node rule (locked + tested):** drop a node missing `namespace` or `key` (unusable as a pill); `name` falls back to `namespace.key`, `type`/`id` default to `""` (display-only). The live `admin.graphql` call is not unit-tested (mocked at the boundary).
  - `app/shopify/metafieldDefinitions.test.ts` (new) — **9 tests** over the pure mapper: normal multi-edge maps in order; empty/missing `edges` → `[]`; non-object input → `[]`; drops nodes missing `namespace`/`key`; `name` falls back to `namespace.key`; `type`/`id` default to `""`; skips malformed edges/nodes without throwing; does not mutate the source JSON. Suite now **104 tests** (was 95).
  - `app/routes/app.metafield-definitions.tsx` (new) — a **resource-route loader only, no default export** at `/app/metafield-definitions` (under `/app` so `authenticate.admin` resolves the embedded session). Calls `fetchProductMetafieldDefinitions(admin)` and returns the project's standard shape: `{ ok: true, definitions }` on success, `{ ok: false, error }` on a caught GraphQL/network error (it does **not** rethrow, so a failed fetch degrades the picker rather than crashing the editor route). Auto-registered via `flatRoutes()`.
  - `app/routes/app.templates_.$id/SpecTableEditor.tsx` — a `useFetcher<typeof loader>()` triggers `fetcher.load("/app/metafield-definitions")` **once on the first modal open** (both create and edit paths call `ensureMetafieldDefinitions()`, guarded by a `metafieldsRequested` state flag so reopening never refetches; the result is cached for the editor's lifetime). A **status region** sits in the modal body **below** the native-field list (a `<s-divider>` + a "Metafields" heading), reflecting the fetch: **loading** → `<s-spinner>` + "Loading metafields…" (shown while `fetcher.state !== "idle"` or data is still undefined, incl. a Retry holding stale data); **error** → `<s-banner tone="critical">` + a **Retry** `<s-button>` that re-issues `loadMetafieldDefinitions`; **empty** → "This store has no product metafield definitions."; **loaded** → "N metafield(s) available. Selecting them arrives in the next step." — a count confirmation, **not** selectable choices. The native search + list and the Insert/Update commit paths are untouched; the status region never gates the primary button.
  - **Locked the Step 8 doc's open questions:** (1) **Pagination cap** — page through all with `first: 250`, hard cap MAX_PAGES = 10 (≈2500, far above any realistic per-shop ceiling), `console.warn` on cap-hit (no silent truncation). (2) **Malformed-node rule** — drop nodes missing `namespace`/`key`; default the display-only fields (tested). (3) **Status-region placement** — below the native list, behind a divider, so Step 9 converts it into the metafield section without re-layout (verified in-browser it doesn't push the native list or fight scroll). (4) **Refetch policy** — once-per-editor-mount confirmed; no manual refresh in MVP. (5) **`type` field** — kept; Step 9 will decide if the icon/hint needs it.
  - **Verified live** (dev store, `TS template`, dev sample rows): clicking into a value cell enabled **Insert field**; opening the modal fired the fetch once and showed the **loading** spinner ("Loading metafields…"), which resolved to **"3 metafields available. Selecting them arrives in the next step."** below the unchanged 13-field native list; **reopening showed the loaded count immediately with no spinner** (no refetch, cache confirmed); the **Step 7 search still filters** ("price" → Price + Compare-at price, metafield region intact below); **the commit path is unregressed** — selecting Price + Insert dropped a `Field · price` pill at the saved caret; the **`Rows: 2 / 200` counter never changed**; **no console errors** (top-frame, incl. no `InvalidStateError` from the Step 7 view-transition gotcha). The **error/Retry path** is wired (banner + Retry button) and passes typecheck/build but was **not force-tested in-browser** (inducing a real GraphQL/network failure in the live embedded app is impractical without disrupting the session) — its loading and loaded siblings are confirmed live. `npm run typecheck`, `lint`, `build`, and `test:run` (104 tests) all pass. Still local React state only — no Save/persist yet. **No reducer action added or changed**; `INSERT_VALUE_PART_AT` / `SET_VALUE_PART` and the `ValuePart` union are unchanged.

- **Editor Step 9 complete** (metafield section in the modal — live data; Session 2026-06-17). See `context/features/10-editor-step9-metafield-section.md`. Step 8's read-only fetch is now a **selectable metafield section** below the native fields, covered by the same Step 7 search; selecting a metafield + Insert drops a real **`METAFIELD` pill** carrying `namespace` + `key` — the finished "smart pill." **No reducer change** (reuses `INSERT_VALUE_PART_AT` / `SET_VALUE_PART`), no schema change, no persistence. Built and **browser-verified end to end** in the real embedded app. Built:
  - `app/utils/shopifyFields.ts` — extracted the Step 7 match core into a private `matchesQuery(needle, ...haystacks)` (behaviour-preserving for `filterNativeFields`, its tests unchanged) and added the pure, unit-tested **`filterMetafieldDefinitions(definitions, query)`**: empty/whitespace → full list in order; otherwise a case-insensitive substring match against the human `name` **and** the `namespace.key` token with every non-alphanumeric run normalised to a space (so `custom.battery_life` matches "battery", "battery life", "custom"). Takes the fetched array as an argument (data passed in, not module-owned — the module owns the rule, via a type-only `MetafieldDefinitionSummary` import); returns a fresh array, never mutates. 15 new tests in `shopifyFields.test.ts` (name/token match, normalisation, case/trim, no-match, empty source, no-mutation, fresh-array) — suite now **116 tests** (was 104).
  - `app/routes/app.templates_.$id/SpecTableEditor.tsx` — replaced `selectedField: string | null` with a discriminated **`FieldSelection`** (`{ kind: "native"; field }` | `{ kind: "metafield"; namespace; key }` | `null`) so the two `<s-choice-list>`s (native, then a `<s-divider>` + "Metafields" heading + metafield list) are **mutually exclusive**: whichever kind is set makes the other list's controlled `values` empty, so only one radio is ever checked. The Step 8 loaded-count text is now the metafield `<s-choice-list>` of `filterMetafieldDefinitions(definitions, searchQuery)` (choices labelled by `name`, keyed by `id`, valued by `namespace.key`); loading / error+Retry / empty-store branches are carried unchanged from Step 8. `handleSelectMetafield` decodes the picked value back to a definition by **lookup** in the loaded list (never string-splitting, so a `.` in a key can't corrupt the pair). `handleCommit` builds a `SHOPIFY_FIELD` **or** `METAFIELD` part from the selection — create → `INSERT_VALUE_PART_AT`, edit → `SET_VALUE_PART`, caret after the pill via `pendingCaretByRowRef`, all paths unchanged. `onEditPart` was generalised to pass the **clicked `ValuePart`** (not a native-only token); the new module-level `partToSelection(part)` pre-selects a METAFIELD by namespace/key and a native field by token (gated by `findNativeField`), **resolving the Step 6 leftover** (a clicked metafield pill now pre-selects). The primary button gates on `selection !== null`; `selection`/`editTarget`/`searchQuery` reset on every open and Cancel.
  - **Combined empty state (locked):** dropped the native-only "No fields match"; a **single** "No fields match" shows only when a non-empty query empties **both** loaded lists, with the Metafields heading kept visible so the section stays discoverable.
  - **Resolved the doc's open questions:** (1) metafield choice shows **`name` only** (matches the iconless native list); `type` stays on the summary for a future hint. (2) **Pre-selection of a non-loaded metafield holds** — confirmed live: editing the dev-sample `custom.battery_life` pill (not one of this shop's real definitions) opens Edit with no radio checked but **Update enabled** (selection held), while editing the real `snowboard_length` pill opens **pre-checked and visible**. (3) Combined-empty placement verified below both sections, no scroll fight. (4) The shared `<s-choice-list>` round-trips correctly as the metafield children appear/disappear under live data + filtering.
  - **Verified live** (dev store, `TS template`, dev sample rows): the modal's metafield section rendered the shop's **3 product definitions** (Snowboard binding mount, Snowboard length, Demo Source Info) as a selectable radio list below the 13 native fields; selecting **Snowboard length** enabled Insert; **search filters both lists** ("snow" → natives gone, 2 snowboard metafields, selection persists = keep-selection); "zzz" → **single combined empty state** with the heading still visible and Insert still enabled (held selection); **Insert dropped a `Metafield · snowboard_length` pill at the saved caret** (carrying namespace + key); clicking the new pill reopened **Edit field** with Snowboard length **pre-selected + visible** and the button **Update**; picking **Price** (native) **deselected** the metafield (mutual exclusivity) and **Update converted it in place** to `Field · price` (surrounding text + the `battery_life` pill intact); the `Rows: 2 / 200` counter never changed; **no console errors** (top frame). `npm run typecheck`, `lint`, `build`, and `test:run` (116 tests) all pass. **No reducer action added or changed**; `data-model.md` and the `ValuePart` union unchanged; still local React state only — no Save/persist yet. This **completes the smart pill** (Steps 5–9); next is Step 10 (mouse drag reorder).

- **Editor Step 9.5 complete** (Save + metaobject sync spike; Session 2026-06-19). See `context/features/11-editor-step9.5-save-metaobject-spike.md`. The first persistence slice: the editor now **saves to Postgres**, **syncs an app-owned Shopify metaobject**, and the action **reads the metaobject back** to confirm the row JSON round-trips. **Browser-verified end to end** in the real embedded app. The two re-homed Save-boundary follow-ups (Step 3 findings #4 + #1) are landed. Built:
  - `app/utils/rowsSerialize.ts` (new, pure, unit-tested) — **`parseRows(unknown): EditorRow[]`** narrows persisted/submitted rows per row (every `ValuePart` variant incl. `LINE_BREAK`; drops rows with no `id` / unknown `rowType`; drops malformed parts but keeps the row with ≥1 TEXT; repairs an empty key from the label), **replacing the old `normalizeRows` cast** (finding #4 resolved); runs at **both** the loader and, server-side, the action (the server never trusts the client payload). **`finalizeRowKeys(rows, provisionalIds)`** slugs only provisional rows and never re-derives a finalized key; **`reconcileRowKeys(incoming, persisted)`** decides provisional-ness **server-authoritatively** = "is this row id already persisted?" (a persisted row keeps its key even if the client re-sends a stale provisional key or the label changed; a new row's key is slugged from its label). This **replaced** the spec's client-tracked id-set (fragile across reloads) — same intent, stronger guarantee (finding #1 resolved). `normalizeRows` was removed (call sites use `parseRows`).
  - `app/models/template.server.ts` — **`saveTemplateForShop(shopId, id, { rows, name?, status? })`**: shop-scoped (reads via `where: { id, shopId }` first — unowned/unknown → `{ ok: false }`, no write), re-checks the **200-row cap server-side** against the shared `MAX_TEMPLATE_ROWS`, re-validates with `parseRows`, finalizes keys with `reconcileRowKeys`, updates by id; `name`/`status` validated + written only when provided (shared `validateName`/`resolveStatus`, factored out of `createTemplateForShop`). **`setTemplateMetaobjectRef`** (shop-scoped `updateMany`) stores the GID + handle. `shop.server.ts` gained **`setShopMetaobjectDefinitionGid`**.
  - `app/shopify/metaobjects.server.ts` (new) — app-owned metaobject sync, same conventions as `metafieldDefinitions.server.ts` (`#graphql` strings **validated with `validate_graphql_codeblocks` at 2025-10**, pure response narrowing exported + unit-tested, live calls mocked). Type **`$app:appx_spec_table`** (app-reserved — exclusive use, data safety), `access { admin: MERCHANT_READ_WRITE, storefront: PUBLIC_READ }`, fields `template_id`/`status`/`updated_at` = `single_line_text_field` and `rows`/`styling` = `json`. `ensureSpecTableDefinition` (create once per shop; `metaobjectDefinitionByType` is the idempotent lookup + create-race recovery), `upsertSpecTableMetaobject` (entry handle `template-{id}`, `rows` = `JSON.stringify(finalizedRows)`), `readSpecTableMetaobjectRows` (round-trip read back).
  - `app/routes/app.templates_.$id/route.tsx` — the `action`'s non-`new` branch now reads the JSON payload, saves to Postgres, then (best-effort, after the durable write) ensures the definition + upserts the metaobject + stores the GID/handle + **reads it back** and returns `roundTripOk`; a sync failure returns a non-fatal `syncError` (rows stay saved). `SpecTableEditor.tsx` — App Bridge **contextual save bar** (`<SaveBar open={isDirty}>`), dirty-tracked vs a saved baseline; Save submits rows as JSON via `useFetcher` (`encType: "application/json"`); Discard remounts the editor (route-level nonce key) to the persisted rows — **no new reducer action**. The dev-only `devSampleRows` scaffolding was **removed** (its job ended when Save landed). `name`/`status` ride the save unchanged (no editor UI to change them yet — a later Settings slice).
  - **Verified live** (dev store, `TS template` `cmq69zsql0002vploqlkld2s3`): authored a row "Battery Life" = "Up to `[Field · vendor]`" + a "Performance" section, Saved → the contextual bar cleared and a **"Saved — storefront round-trip verified"** toast appeared (the metaobject read-back matched the saved rows). **Postgres confirmed directly** (Neon `proud-hat-02103652`): `Template.rows` persisted as typed `EditorRow[]` with the provisional `row` key **finalized to `battery_life`** and `section` → `performance`, `valueParts` intact (`TEXT` + `SHOPIFY_FIELD vendor` + `TEXT`); `shopifyMetaobjectGid` + `shopifyMetaobjectHandle` (`template-cmq69zsql0002vploqlkld2s3`) stored on the Template; `Shop.metaobjectDefinitionGid` set. A **second save kept `battery_life`** (not re-derived, though the client re-sent the stale `row` key) — the key-preservation invariant, live. **Discard** reverted the editor to the persisted rows; a **hard reload** re-rendered them via the loader's `parseRows`. **No console errors.** `npm run typecheck`, `lint`, `build`, and `test:run` (**150 tests**, was 116) all pass. The **row JSON shape survived unchanged** — no `valueParts` reshape was needed, so the spike's key risk (a shape change feeding back into Steps 10–13) is cleared. The deployable Theme App Extension Liquid renderer is deferred to the storefront/assignment slice (its boundary); the verified storefront read syntax is recorded in `data-model.md` §9.

- **Editor Step 10 complete** (mouse drag reorder with `@dnd-kit`; Session 2026-06-19). See `context/features/12-editor-step10-mouse-drag-reorder.md`. The ⠿ gutter handle — inert since Step 2 — now reorders rows by **mouse drag**; one new pure reducer action (`MOVE_ROW`), **no change to the Step 9.5 persistence contract**. Built and **browser-verified end to end** in the real embedded app. Built:
  - `app/utils/rows.ts` — added the **`MOVE_ROW { activeId, overId }`** reducer action + a private, framework-free **`arrayMove<T>(list, from, to)`** helper (kept local so `rows.ts` stays UI-library-free per its module header; matches `@dnd-kit/sortable`'s arrayMove semantics — `to` is the target index in the original array). The case resolves **both indices from ids** via `findIndex` and **returns the same array reference** on any no-op (unknown id, or `from === to`) so a same-spot drop never flips the dirty flag; otherwise a pure array-move. **No `key`/`id` mutation** (display order *is* the array index — data-model §6/§11; a finalized key is never re-derived — §12) and **no cap check** (a reorder cannot grow the array). 12 new unit tests in `rows.test.ts` (move down/up, adjacent swap, to-first/to-last, the three no-op same-reference cases, a `SECTION_HEADER` moving like any row, a data row moving past a section without absorbing it, id/key preservation, purity) — suite now **162 tests** (was 150).
  - `app/routes/app.templates_.$id/SpecTableEditor.tsx` — wired `@dnd-kit`: a `DndContext` (single **`PointerSensor`**, `activationConstraint: { distance: 4 }` so a handle click ≠ a drag; **no `KeyboardSensor` — that's Step 11**) + `SortableContext` (`items` keyed by `row.id`, `verticalListSortingStrategy`) wrap the mapped rows (both render no DOM, so the `<s-stack>` gap is preserved and rows stay React-keyed by id → caret/focus survive a reorder). `EditorRowItem` calls `useSortable({ id: row.id })` and applies `setNodeRef` + `CSS.Transform.toString(transform)` + `transition` to the plain wrapper `<div>` (Polaris `<s-*>` hosts untouched). `RowGutter` gained the handle wiring: the sensor **`listeners` + `setActivatorNodeRef` are spread on the ⠿ handle ONLY** (so the value cell keeps its caret/selection behaviour), while the dnd-kit **`attributes` are deliberately NOT spread** — the handle stays `aria-hidden` and non-focusable this step (mouse-only, no aria-hidden+focusable conflict). `onDragEnd` dispatches `MOVE_ROW` only when `over` exists and `active.id !== over.id`. The Save/dirty-tracking, `action`, model helper, and metaobject sync are **all unchanged** — a reorder rides the existing Step 9.5 path.
  - `app/routes/app.templates_.$id/SpecTableEditor.module.css` — `.dragHandle` gained `touch-action: none` (hand the gesture to the pointer sensor, not the scroller) + a `grabbing` cursor while dragging; new `.rowDragging` lifts the dragged row (`z-index`, slight `opacity`). Polaris-token / `currentColor` / rem only — no hex.
  - `package.json` — added `@dnd-kit/core@^6.3.1`, `@dnd-kit/sortable@^10.0.0`, `@dnd-kit/utilities@^3.2.2` (the one new dependency family `data-model.md` §13 anticipated).
  - **Verified live** (dev store, `New template` `cmqgl3ffu0001vpgctnyn8rda`, rows Display[section] / Size / Type[multiline]): dragged the multiline **Type** row's ⠿ handle up onto **Size** → order became Display → Type → Size, **the two-line value intact** and `Rows: 3 / 200` unchanged; the **contextual save bar appeared**; clicking the moved value cell **re-enabled Insert field + Duplicate** (the caret bridge still fires post-reorder); **Save** cleared the bar and toasted **"Saved — storefront round-trip verified"** (the reorder round-tripped Postgres → metaobject → readback). **Postgres confirmed directly** (Neon `proud-hat-02103652`): persisted `rows` in the new order with **keys NOT re-derived** (`display`/`type`/`size` kept their id↔key pairing despite moving — data-model §12 live) and the `LINE_BREAK` intact. Dragging the **Display section** to the bottom moved only that header (**sections drag as ordinary rows, no children follow**); **Discard** reverted to the saved order; a **hard reload** re-rendered the persisted order via the loader's `parseRows`. **No console errors** (admin top frame). An adversarial **4-dimension review workflow** (reducer correctness / dnd-kit wiring / persistence contract / regression+memoization+a11y) returned **zero findings**. `npm run typecheck`, `lint`, `build`, and `test:run` (162 tests) all pass. **Mouse-only is the deliberate scope** — the handle stays `aria-hidden`; **Step 11** adds the keyboard sensor + focusable handle + SR announcements **before** App Store review. **No existing reducer action changed; `data-model.md` and the `ValuePart` union unchanged; the Step 9.5 persistence contract is untouched.**

- **Editor Step 11 complete** (keyboard reorder + accessibility; Session 2026-06-19). See `context/features/13-editor-step11-keyboard-reorder-a11y.md`. The Step 10 mouse-only reorder is now **fully keyboard-operable and screen-reader-announced** — closing the "do not ship mouse-only drag" requirement in `code-standards.md` before App Store review. **No reducer change, no new dependency, no change to the Step 9.5 persistence contract** (a keyboard drop fires the same `MOVE_ROW` → same Save path as a mouse drop). Built and **browser-verified end to end** in the real embedded app. Built:
  - `app/utils/reorderAnnouncements.ts` (new, pure, unit-tested) — owns the screen-reader announcement **copy** (the component does the dnd-kit glue; module imports the `EditorRow` **type** only, no `@dnd-kit` dependency in `utils/`, mirroring `shopifyFields.ts`). Exports `describeRow(row, index)` (a row's natural descriptor — "Battery Life row" / "Display section", with a 1-based positional fallback "row 3" / "section 3" when the label is blank) and the four message builders `announceReorderStart/Over/End/Cancel(rows, activeId[, overId])` returning row-aware strings with **1-based positions** ("position 3 of 9"). Positions read the **current (pre-move) rows**: since `MOVE_ROW` only dispatches on drop, the `over` row's slot is exactly where the dragged row lands (`arrayMove` semantics), so "now over / dropped at position N" is correct. 16 new tests in `reorderAnnouncements.test.ts` (descriptor label/section/blank-fallback/trim; start instructions; over/end position math; sentence-start capitalization; null/unknown-over degradation; cancel wording) — suite now **178 tests** (was 162).
  - `app/routes/app.templates_.$id/SpecTableEditor.tsx` — added a **`KeyboardSensor`** (with `sortableKeyboardCoordinates` so the arrow keys step the vertical list; Space/Enter pick up & drop, Escape cancels) as a second sensor on the **existing** `useSensors`/`DndContext` — `onDragEnd` is unchanged, so the keyboard drop reuses the Step 10 `MOVE_ROW` path. The ⠿ handle in `RowGutter` became a **focusable native `<button type="button">`**: it now spreads the dnd-kit **`attributes`** (role/tabIndex/`aria-roledescription="sortable"`/`aria-describedby`/`aria-pressed`) that Step 10 deliberately withheld, **drops `aria-hidden`**, gets an `aria-label` ("Reorder <descriptor>" via `describeRow`), and marks the inner `<s-icon aria-hidden="true">` presentational so the labeled button isn't double-announced. `EditorRowItem` destructures `attributes` from `useSortable` and threads it + `reorderLabel` to `RowGutter` (RowGutter stays `memo`'d; the new props are a stable string + the per-row hook object, so the "only edited/active row re-renders" guarantee holds). The `DndContext` gained `accessibility={{ announcements, screenReaderInstructions }}` — row-aware `announcements` built with `useMemo` reading a `rowsRef` (current rows, no stale closure) from the pure helper, and a one-line `REORDER_INSTRUCTIONS`. Save/dirty-tracking, the `action`, the model helper, and the metaobject sync are **all unchanged**.
  - `app/routes/app.templates_.$id/SpecTableEditor.module.css` — `.dragHandle` gained the native-`<button>` chrome reset (`appearance/background/border/margin/padding/font/color` → none/inherit, so it still reads as the bare ⠿ icon) and a **`:focus-visible`** ring (`outline: 0.125rem solid currentColor` + offset, opacity lifted to full on focus) so keyboard focus is clearly visible. The Step 10 `cursor: grab/grabbing`, `touch-action: none`, and `.rowDragging` rules are unchanged. **No hex** — `currentColor` / rem only.
  - **Verified live** (dev store, `New template` `cmqgl3ffu0001vpgctnyn8rda`, keyboard only): Tab/click-focused the **Size** handle (focus ring shows on **keyboard** focus, correctly not on mouse — `:focus-visible`); **Enter** picked it up (row lifted), **ArrowDown** moved it below Resolution, **Enter** dropped it → order became Display → Resolution → Size → …, **save bar appeared**, and **focus stayed on the moved handle** (dnd-kit `restoreFocus`). **Save** persisted; **Postgres confirmed directly** (Neon `proud-hat-02103652`): rows in the new keyboard order with **keys NOT re-derived** (`resolution`/`size`/… kept their id↔key pairing — data-model §12 live). **Escape mid-drag cancelled** — the row returned to its original slot with **no dirty state** (no `MOVE_ROW` dispatched during a drag). A **section** header (Display) reordered by keyboard, **moving only that header** (no children follow). **Mouse drag from Step 10 still works** (the `PointerSensor` is unaffected); the value-cell **caret bridge is unregressed** (clicking a value cell re-enabled Insert field + Duplicate; the multiline value stayed intact); **Discard** reverted to the saved order. **No top-frame console errors** (where the Step 7 `InvalidStateError` family would appear). An adversarial **4-dimension review workflow** (dnd-kit wiring / a11y-ARIA / announcement-helper logic / regression+memoization+CSS) returned **one confirmed finding** — the inner `<s-icon>` was missing the spec-required `aria-hidden` — now **fixed**; the other three dimensions returned zero. `npm run typecheck`, `lint`, `build`, and `test:run` (178 tests) all pass. **Limitation:** live screen-reader audio / AOM attribute inspection is blocked by the cross-origin embedded iframe, so the keyboard behaviour + focus were verified functionally and the announcement copy via unit tests (not by hearing NVDA read the live region). **No reducer action added or changed; `data-model.md` and the `ValuePart` union unchanged; the Step 9.5 persistence contract is untouched.** This **completes the reorder feature** (Steps 10–11); next is clipboard paste (Steps 12–13).

- **Testing — Phase 1 (unit tests) set up** (Session 2026-06-14). First automated tests in the project (previously none). Built:
  - Added **Vitest** (`vitest` + `@vitest/coverage-v8`, dev deps), a standalone `vitest.config.ts` (Node env; `include: app/**/*.{test,spec}.{ts,tsx}`; v8 coverage) that deliberately does **not** load the React Router Vite plugin — `reactRouter()` expects the framework build context and breaks under Vitest. Added `test` / `test:run` / `test:coverage` scripts to `package.json`.
  - `app/utils/rows.test.ts` — **34 tests** covering every `rowsReducer` action (`ADD_ROW`, `ADD_SECTION`, `DUPLICATE_ROW`, `DELETE_ROW`, `SET_LABEL`, `SET_VALUE_TEXT`, `REMOVE_VALUE_PART`, `INSERT_VALUE_PART`), the `MAX_TEMPLATE_ROWS` cap (reducer is the gate: refuses all three row-creating actions at the cap, allows the row that lands exactly on it), reducer purity (input array not mutated), insert-below-active vs. append, duplicate's fresh id + unique key + independent `valueParts` array, `SET_LABEL` never rewriting `key`, value-part actions no-op on `SECTION_HEADER`, and the helpers (`slugifyKey`, `uniqueKey`, `normalizeValueParts`, `normalizeRows`, `newRowId`, `placeholderMetafieldPart`).
  - **Coverage of `rows.ts`: 100% statements / lines / functions, 94.5% branches.** `npm run test:run`, `typecheck`, `lint`, and `build` all pass clean (test files are not bundled into the app build — module count unchanged). Documented the testing convention in `code-standards.md` (new "Testing" section).

- **Testing — Phase 2 (shop isolation / data-safety) complete** (Session 2026-06-14). Tests for the data-access layer with a **mocked Prisma client** (no real DB): the `app/db.server.ts` default export is replaced with `vi.fn()` spies via `vi.hoisted` + `vi.mock`, so the tests assert the *shop-scoped query our code builds* (priority #1 invariant) rather than exercising Postgres. Added:
  - `app/models/template.server.test.ts` — `listTemplatesForShop` (scoped `where: { shopId }`, valid-status filter only, rowCount mapping), `countTemplatesForShop` (shop-scoped), `createTemplateForShop` (empty / missing / over-100-char name rejected with no write; name trimmed; unknown status → DRAFT; valid status passthrough; `ok:true` on success; `ok:false` on a DB throw), and **`getTemplateByIdForShop` shop isolation**: missing id → null without querying; a wrong-shop fetch passes `where: { id, shopId }` and returns null (one shop cannot read another's template); an owned fetch returns the row.
  - `app/models/shop.server.test.ts` — `upsertShop` (already-installed short-circuit with no write; first-install create; reinstall flips `isInstalled` back on; **P2002 unique-constraint race recovers via `update`**; non-P2002 errors rethrow) and `markShopUninstalled` (marks only `isInstalled: true` rows for the domain, stamping `uninstalledAt`; a repeat webhook delivery is a safe idempotent no-op).
  - **Coverage: both model files at 100% statements / branches / functions / lines.** 55 tests across 3 files; `npm run test:run`, `typecheck`, `lint`, and `build` all pass clean. The uninstall webhook **route** (`webhooks.app.uninstalled.tsx`, which also calls `db.session.deleteMany`) is deferred to **Phase 3**, where the Shopify-auth mock (`testConfig()`) is set up once for all route/loader/action tests.

## In Progress

- None active. **Step 11 — Keyboard reorder + accessibility** is complete and **browser-verified end to end** in the real embedded app: the ⠿ gutter handle is now a focusable native `<button>` driven by a `KeyboardSensor` (Space/Enter pick up & drop, arrows move, Escape cancels), with row-aware screen-reader announcements + instructions on the `DndContext`; the keyboard drop reuses the **unchanged** Step 10 `MOVE_ROW` + Step 9.5 Save path (keys never re-derived — verified directly in Postgres), sections reorder by keyboard, Escape leaves no dirty state, mouse drag is unregressed, and there are no top-frame console errors. This **completes the reorder feature** (Steps 10–11) and closes `code-standards.md`'s "do not ship mouse-only drag" requirement before App Store review. The contenteditable value surface and the smart-pill picker (Steps 5–9) are unregressed. **Step 12 (parse pasted clipboard tables)** is next up. *(One adversarial-review finding — a missing `aria-hidden` on the handle's inner `<s-icon>` — was fixed; full SR-audio/AOM verification is limited by the cross-origin iframe.)*

---

## Next Up

The editor build follows the 13-step order with one inserted step (Steps 1–11 done; **Step 9.5** added 2026-06-17; **Steps 10–11** completed 2026-06-19). Remaining steps:

- ~~**Step 10 — Mouse drag reorder**~~ — **DONE (2026-06-19).** `@dnd-kit` (`core` + `sortable` + `utilities`) installed; ⠿ gutter handle wired via a mouse-only `PointerSensor`; `MOVE_ROW` id-keyed array-move on the reducer. Does not touch the Step 9.5 persistence contract. See the "Editor Step 10 complete" entry under Completed.
- ~~**Step 11 — Keyboard reorder + accessibility**~~ — **DONE (2026-06-19).** `KeyboardSensor` (+ `sortableKeyboardCoordinates`) added to the existing `DndContext`; ⠿ handle is now a focusable native `<button>` spreading the dnd-kit `attributes` (no longer `aria-hidden`) with an `aria-label` + presentational inner icon; row-aware `accessibility={{ announcements, screenReaderInstructions }}` from the new pure `app/utils/reorderAnnouncements.ts`. No reducer/dependency/persistence change. See the "Editor Step 11 complete" entry under Completed.
- **Step 12 — Parse pasted clipboard tables:** capture paste and parse the HTML table / TSV into a 2-D grid in a pure, testable helper (Excel / Google Sheets / web tables). Parse + log only — no rows inserted.
- **Step 13 — Bulk-insert rows from paste:** `PASTE_ROWS` action (col 1 → Label, remaining cols → joined plain-text Value), enforcing the 200-row cap on paste (truncate + tell the merchant what was dropped) using the same shared `MAX_TEMPLATE_ROWS`. *Reuse the Step 9.5 key-finalization path for pasted rows — don't fork a second keying path.*

- **Deferred / no longer numbered editor steps:** undo/redo, WYSIWYG storefront styling, and the Desktop / Tablet / Mobile viewport toggle move to the later styling/persistence slice (not the interaction build above).
- **Remaining first-slice work after the editor:** ~~wire Save, save rows to Postgres, sync the metaobject~~ (done in Step 9.5) → assign a template to one product, write the product metafield, and **build + deploy the Theme App Extension app block** that renders the table on the storefront (incl. the deployable Liquid readback deferred from Step 9.5.4 — see the §9 note in `data-model.md` for the verified read syntax + the app-reserved-metaobject access caveat).

## Step 3 Follow-ups (tracked)

Logged during the Step 3 review; deliberately not fixed there to keep the step a behavior freeze (no new reducer action, no new UI surface). Each is tagged with the step that owns it.

- **[Step 4 — RESOLVED]** The three caret/a11y follow-ups below are fixed by the Step 4 contenteditable rewrite (see the "Editor Step 4 complete" entry): (1) caret/focus loss on pill remove & after insert (the single surface owns caret/selection, restored from a linear index on structural edits); (2) duplicate `aria-label`s on multi-TEXT cells (one labelled `role="textbox"` host + per-token accessible names); (3) the fiddly empty TEXT segment (one continuous surface — no per-segment inputs).
- **[Step 9.5 — RESOLVED] `normalizeRows` did no per-row validation (finding #4).** Fixed: `parseRows(unknown): EditorRow[]` in `app/utils/rowsSerialize.ts` narrows per row (every `ValuePart` variant, drops malformed rows/parts, keeps ≥1 TEXT, repairs an empty key from the label) and runs at **both** the loader and, server-side, the action; `normalizeRows` was removed. Unit-tested.
- **[Step 9.5 — RESOLVED] Finalize provisional row keys at Save (finding #1 follow-through).** Fixed: `finalizeRowKeys` + `reconcileRowKeys` (`app/utils/rowsSerialize.ts`). Provisional-ness is decided **server-authoritatively** ("is this row id already persisted?"), not by a key-string pattern and not by the spec's client-tracked id-set (fragile across reloads) — a persisted key is never re-derived (even after a label change, or a label that slugs to `row`/`section`); a new row's key is slugged from its label. Step 13's paste reuses this path. Verified live (provisional `row` → `battery_life`, kept on re-save). Unit-tested.
- **[Later, low priority] `insertActive` optimism at the cap.** `insertActive` sets `scrollTargetRef`/`activeRowId` before the reducer runs; at the cap the reducer no-ops (returns the same array), so `activeRowId` could point at a never-added row and the scroll ref stays stale. Unreachable today (all row-creating buttons are `disabled` at the cap), but if a later step adds a keyboard shortcut / programmatic add that bypasses the disabled button, guard `insertActive` on `!atCap` or only set active/scroll when `rows.length` actually grows.

## Open Questions

- ~~Exact Shopify Admin API mutations for creating/updating the app-owned metaobject definition and entries~~ **RESOLVED (Step 9.5):** `metaobjectDefinitionCreate` (type `$app:appx_spec_table`, access admin RW + storefront PUBLIC_READ) / `metaobjectDefinitionByType` / `metaobjectUpsert` (handle `template-{id}`) / `metaobjectByHandle`, validated at 2025-10 — see `app/shopify/metaobjects.server.ts` + `data-model.md` §10.
- **Exact Liquid syntax for reading the product metafield and metaobject payload in the Theme App Extension** — *partially resolved (Step 9.5):* the metaobject read syntax is `{{ metaobjects[type][handle] }}` with `rows.value` iterable (recorded in `data-model.md` §9); still open for the storefront slice: the **app-reserved** (`$app:`) metaobject's storefront access pattern (likely a metaobject-reference product metafield, not a raw handle lookup), and the product-metafield → template pointer.
- Best storefront event strategy for selected variant changes across Shopify themes
- Exact UX for preventing or warning about assignment conflicts in MVP

## Session Notes

- Build the complete create/save/sync/assign/render flow before expanding advanced styling, import/export, AI, analytics, or bulk assignment.

- **Step 10 Decisions (Session 2026-06-19):** Built + browser-verified mouse drag reorder (`@dnd-kit`). Notable points:
  - **`@dnd-kit` works cleanly in this embedded Polaris CDN build** (a positive vs the [[polaris-web-component-gotchas]] track record): `DndContext` / `SortableContext` / `useSortable` with a single `PointerSensor` reorder rows with **no console errors**; `setNodeRef` + `transform` on the plain wrapper `<div>` (not `<s-*>` hosts) and `listeners` on the ⠿ handle leave the contenteditable value surface and the Polaris chrome untouched. A `DragOverlay` was **not** needed — the in-place transform did not disturb the caret (the doc's open question resolved in favour of the simpler path). Verification method: `left_click_drag` via Claude-in-Chrome on the `shopify app dev` preview reliably triggers the PointerSensor; persistence cross-checked against Neon (`proud-hat-02103652`).
  - **`MOVE_ROW` is a pure, id-keyed array-move; reorder is NOT re-keying.** Display order = array index (data-model §6/§11); the action never touches `key`/`id`, so a finalized key (`type`, `size`) survives a move — confirmed directly in Postgres. No-ops return the **same array reference** so a same-spot drop never marks the editor dirty. `arrayMove` is kept **local** to `rows.ts` (not imported from `@dnd-kit/sortable`) to preserve the module's framework-free invariant.
  - **Mouse-only is deliberate; the handle stays `aria-hidden`.** The dnd-kit `attributes` (role/tabIndex) are intentionally **not** spread this step, to avoid an aria-hidden+focusable conflict on a handle that has no keyboard affordance yet. **Step 11** spreads them + drops `aria-hidden` + adds the `KeyboardSensor` and announcements (a Shopify-review requirement) on top of this working base.
  - **An adversarial 4-dimension review workflow** (reducer correctness / dnd-kit wiring / persistence contract / regression+memoization+a11y) returned **zero findings** before browser verification.

- **Step 9.5 Decisions (Session 2026-06-19):** Implemented + browser-verified the first persistence slice. Two deliberate, documented deltas from the feature spec (`context/features/11-…`), same intent / stronger mechanism — see that doc's "Implementation outcome" section:
  - **Key finalization is server-authoritative, not a client id-set.** `reconcileRowKeys(incoming, persisted)` decides provisional-ness by "is this row id already persisted?" — robust across reloads and unfakeable by the client; the spec's editor-state `Set<rowId>` was rejected as fragile. "Never re-derive a finalized key" (data-model §12) holds even for a label that slugs to `row`/`section`.
  - **Round-trip proof is an Admin-API `metaobjectByHandle` read-back, not a Liquid block.** It runs on every save and auto-verifies (the "round-trip verified" toast); it's the only surface testable in an admin-only session. The deployable Theme App Extension Liquid renderer belongs to the storefront/assignment slice (Step 9.5's own boundary) and is deferred there — not skipped, re-homed.
  - **The row JSON shape survived the round trip unchanged** — the spike's key purpose. No `valueParts` reshape was needed, so nothing feeds back into Steps 10–13; the row contract in `data-model.md` §6–7 is confirmed against real Postgres + a real Shopify metaobject.
  - Metaobject is **app-reserved** (`$app:appx_spec_table`) for data safety; the storefront read path for app-reserved metaobjects (metaobject-reference metafield vs handle lookup) is the one piece left open for the storefront slice.

- **Editor Build Decision (Session 2026-06-10):** The spec-table editor will be a **custom React editor — no AG Grid.** Rationale: the table is only 2 columns, capped at 200 rows; the value cell is a `valueParts` token editor (manual text + dynamic-field pills) with escaping popovers, which a generic data grid models poorly and which `code-standards.md` already forbids fighting. AG Grid would remove almost none of the real work (pill editor, field picker, undo/redo, preview are custom regardless). Decisions locked:
  - **Drag-and-drop:** `@dnd-kit` (`@dnd-kit/core` + `@dnd-kit/sortable`) — one new dependency; keyboard-accessible reordering.
  - **Value editor:** segmented "Insert field" model (text inputs + removable pill chips + field picker).
  - Suggested build order: (1) reducer + static row render + add/delete/duplicate + 200-row cap; (2) segmented value cell + pills; (3) field picker + native Shopify fields (metafield definitions as a sub-step); (4) `@dnd-kit` reorder + keyboard nav; (5) clipboard paste-in of multi-cell tables → bulk row creation; (6) undo/redo + storefront-styled WYSIWYG rendering (incl. viewport toggle) + wire Save (server-side 200-row + `shopId` re-check). **(Later refined into the 13-step order — see the "Editor Roadmap Revision (Session 2026-06-13)" note below.)**

- **Editor UX Decisions (Session 2026-06-11):** Reviewed an app's Excel-like editor and confirmed the structured-row-editor direction. Decisions locked:
  - **WYSIWYG editor, no preview panel:** the editing table renders exactly like the storefront table with the current `TableStyling` at all times. No separate live-preview panel. **(Superseded 2026-06-14 — see "Editor View Toggle Decision" below: the view toggle now has a dedicated Edit segment, and Desktop / Tablet / Mobile are read-only previews, not editable viewports.)**
  - **Clipboard paste is an MVP feature:** pasting a multi-cell table copied from any website / Excel / Google Sheets bulk-creates rows — first pasted column → label, remaining columns → manual TEXT value; 200-row cap enforced on paste. Multi cell copy paste ability should be implemented.
  - **Row cap is configurable:** 200 is the MVP value and may increase post-MVP. Implement it as a single shared constant read by both the editor UI and server-side save validation — never a hardcoded literal.

- **Editor Step 2 Decisions (Session 2026-06-13):** Built and verified the segmented value cell, pills, toolbar, and row gutter in the real embedded app. Decisions locked + open questions resolved:
  - **Open question — pill label format (resolved):** `Metafield · <key>` and `Field · <field>`. Icons: `metafields` for METAFIELD, `product` for SHOPIFY_FIELD. **(Superseded 2026-06-15 — pick-then-insert means pills are never created empty, so the `· choose field` placeholder state is retired; the `Metafield · <key>` / `Field · <field>` labels and icons stand.)**
  - **Open question — empty-value / `hideWhenEmpty` rule (resolved):** purely a storefront concern (Step 6). The editor does not hide or special-case rows whose value is an empty TEXT part; it always keeps ≥1 editable TEXT part in the cell.
  - **Pill = chip + explicit ✕ button (not chip `removable`):** `<s-chip removable>` does **not** paint a visible remove control in the current Polaris CDN build (confirmed in-browser — no ✕ even on hover), even though the `removable`/`onRemove` props exist in `@shopify/polaris-types`. So the pill is `<s-chip color="strong">` + an adjacent `<s-button icon="x">` wired to `REMOVE_VALUE_PART`. Polaris chips also have no blue variant (only `subdued`/`base`/`strong`) and `code-standards.md` forbids overriding component internals, so the mockup's "blue pill" is realized as the `strong` chip tone.
  - **Toolbar uses `<s-stack direction="inline">`, not `<s-button-group>`:** `<s-button-group>` renders a shadow root with no `<slot>` in the current build, so its child buttons disappear (confirmed in-browser). `<s-stack>` projects children correctly.
  - **TEXT segments are native `<input>`:** borderless, content-sized (via the `size` attr), inside an `<s-box>` field container — the token-editor inline flow that `code-standards.md` anticipates Polaris fields can't model. Scoped CSS in `SpecTableEditor.module.css` uses only `currentColor` + rem (no hardcoded hex).
  - **Minor UX follow-up (deferred):** an empty TEXT segment is narrow (~2ch) and fiddly to click to continue typing after an insert. The primary `type → insert → type` flow works; consider auto-focusing the trailing segment right after `INSERT_VALUE_PART` (caret handling is explicitly deferred per the Step 2 feature file). In the revised roadmap this caret/focus work belongs to **Step 4** (the Claude-style inline pill rework), which consolidates the per-segment inputs and owns caret handling — see `context/features/04-editor-step3-review-harden.md` finding #3.

- **Editor Roadmap Revision (Session 2026-06-13):** The original 6-step editor build order (in the 2026-06-10 decision above) was refined into a more granular **13-step** order so each step is independently buildable and verifiable. Key changes:
  - Old Step 3 ("field picker") is now **Step 3 = review & harden Steps 1–2** (no new features) — `context/features/04-editor-step3-review-harden.md`. The picker itself splits across **Steps 5–9**: popover shell → native Shopify fields (static) → search/filter → fetch metafield definitions → live metafield section.
  - The **Claude-style inline pill** rework (link-styled, keyboard-deletable token, no per-pill ✕) is now its own **Step 4**, before the picker inserts into it.
  - `@dnd-kit` reorder splits into **Step 10 (mouse)** + **Step 11 (keyboard / a11y)**; clipboard paste splits into **Step 12 (parse → 2-D grid)** + **Step 13 (bulk insert + cap)**.
  - **Undo/redo, WYSIWYG storefront styling, and the viewport toggle are no longer numbered editor steps** — they move to the later styling/persistence slice alongside wiring Save.
  - Per-step docs live in `context/features/`; the file prefix is step + 1 (e.g. Step 3 → `04-…`) because `01-one-route-editor.md` predates the editor steps.

- **Editor Step 3 (Session 2026-06-13):** Reviewed & hardened Steps 1–2 — full triage, confirmed invariants, and the three comment-only fixes are in the "Editor Step 3 complete" entry under Completed; not-fixed-now items are under "Step 3 Follow-ups (tracked)." Two open questions from the Step 3 doc resolved: **label → key** — derive the slug from the label at **Save (Step 6)**, keeping provisional `row_N` keys during editing and never rewriting a finalized key (keeps `slugifyKey` alive as the Step 6 tool); **section full-width** — already correct (the `2.75rem 1fr` section grid fills the full post-gutter width), and the data-column header intentionally does not get a matching treatment because sections are meant to break the two-column grid. `npm run typecheck` / `lint` / `build` pass, and the core flows were re-verified live in the embedded app (see the Step 3 "Gates" bullet under Completed) — section full-width (finding #2) and the duplicate deep-copy both confirmed, no console errors.

- **Testing strategy adopted (Session 2026-06-14):** The project now has an automated test suite (previously none). Researched how Shopify / React-Router-template developers test (reference: Shopify Remix testing PR #377 — Vitest + Testing Library + Playwright; `testConfig()` helper from `@shopify/shopify-app-react-router/test-helpers`). Decision — follow the testing pyramid and reach **Phases 1→4 before App Store submission**, with E2E as a fast-follow:
  1. **Unit** (pure logic) — **done** (`app/utils/rows.test.ts`; see "Testing — Phase 1" under Completed).
  2. **Shop-isolation / data-safety** (priority #1) — **done** (`template.server.test.ts` / `shop.server.test.ts`): mocked Prisma asserts every read/write is shop-scoped (`getTemplateByIdForShop` never returns another shop's row); covers create-validation, install/reinstall, the P2002 race, and uninstall idempotency.
  3. **Route loaders/actions**: `testConfig()` + React Router 7's `createRoutesStub` (the RR7 equivalent of `@remix-run/testing`).
  4. **Compliance / GDPR webhooks** return 200 and clean up.
  5. **(Later) Playwright E2E** — admin save flow + storefront theme-extension render.
  - DB strategy for integration tests: **mock Prisma first**; a Neon test branch is the later upgrade if real-DB fidelity is wanted. The App Store does not *require* automated tests, but they de-risk exactly what review checks (no console errors, working webhooks, LCP ≤ 2.5s, storefront speed). Note: Polaris web components don't render in jsdom, so editor UI behavior is covered by E2E, not jsdom component tests — the reducer logic is already pure in `rows.ts`. Full strategy doc kept at `~/.claude/plans/there-is-no-automated-encapsulated-yeti.md`. Next testing increment: **Phase 3** (route loaders/actions + the uninstall webhook route, via `testConfig()` + `createRoutesStub`).

- **Editor View Toggle Decision (Session 2026-06-14):** The view toggle is now a **single view state** with four mutually-exclusive segments — **Edit** (pencil + label), a divider, then **Desktop / Tablet / Mobile**. This **supersedes** the 2026-06-11 "stays fully editable in every viewport" decision: only **Edit** is editable; Desktop / Tablet / Mobile are **read-only previews** of how the table renders for shoppers at that width (mobile = stacked label-over-value). Rationale + scope:
  - **Device views are view-only (`.preview`):** all editor chrome is hidden (toolbar, hint, gutter, add-row button, edit box, field picker), the gutter column collapses, and edit highlights / interactions are stripped. The table renders exactly like the storefront. This is a deliberate change to the editor's interaction flow — editing happens **only** in the Edit view, then the merchant switches to a device segment to inspect the storefront result. No separate preview panel (the WYSIWYG-rendering principle is unchanged; what changed is that previews are now read-only and live behind the device segments instead of every viewport being editable).
  - **Preview stand-in values:** read-only device views show resolved placeholder values for dynamic parts (e.g., a stand-in for the Battery Life row) so the preview reads like a real storefront table.
  - Validated in the mockup ([design/spec-editor-mockup.html](design/spec-editor-mockup.html)); the production editor's Desktop/Tablet/Mobile rendering still belongs to the later styling/persistence slice (it was never a numbered editor step), but it must now be built as **read-only**, not editable.

- **Multiline / `LINE_BREAK` decision (Session 2026-06-15):** Studied real merchant spec tables (Startech, Amazon, Walmart, eBay, Best Buy). Confirmed values need **hard-break lines** (author-intended, e.g. a "Features" list, one item per line) on top of automatic soft-wrap, but **no** inline rich formatting, links, or widgets for MVP (links = the roadmap's Phase 4 URL field; star-rating widgets out). Decisions locked:
  - **New `LINE_BREAK` value part** added to `data-model.md` §7 (additive; product-agnostic; ignored by the comparison-readiness invariant and the 200-row cap; renders as a hard break in editor + storefront). Soft-wrap stays free (CSS). This is the **only genuine net-new scope** vs. the committed PRD/roadmap — everything richer (zebra striping, separate header styling, accordion, style presets, links) was already phased on `feature-roadmap.md` as Phase 2/4, so deferring them follows the existing plan rather than cutting scope.
  - **`hideWhenEmpty` is whole-row, never per line** (`data-model.md` §10): a row hides only when its entire resolved value is empty (all dynamic parts empty across all lines); `LINE_BREAK` carries no content. The existing "hide when dynamic parts empty" rule is unchanged — just clarified as whole-row, with no per-line hide logic.
  - **Editor:** hard-break multiline becomes **sub-step 4.4** of the inline-pill step (same `contenteditable` surface), not a standalone step. It **supersedes** the Step 4 doc's "value shape unchanged / no new part type / no new reducer action" absolutes (now scoped to 4.1–4.3). 4.4 may add one caret-aware insert/split action (`INSERT_VALUE_PART` only appends, so it can't express a mid-caret split); deletion reuses `REMOVE_VALUE_PART` + `normalizeValueParts`. The `LINE_BREAK` member of the `ValuePart` union in `app/utils/rows.ts` is added in 4.4, not now. A full editor framework (Lexical) was considered and **rejected** — no committed MVP feature justifies it (paste is row-level bulk-create, not inline-rich; no inline formatting anywhere). See the 2026-06-15 amendment in `context/features/05-editor-step4-inline-pills.md`.

- **Smart-pill creation model — pick-then-insert (Session 2026-06-15):** Reversed the earlier insert-then-fill / placeholder-pill design. A pill is now **created complete**: an **Insert field** toolbar button (disabled when there is no active caret) opens a focus-trapped **modal**; the merchant picks a native field / metafield (Steps 6–9 content) and clicks **Insert** → a complete `SHOPIFY_FIELD`/`METAFIELD` pill drops at the **saved caret**; **Cancel** / Esc / outside-click inserts nothing. Rationale + consequences:
  - **Lower dev risk:** the picker lives entirely **outside** the `contenteditable`, so it never entangles the caret model (the editor's hardest problem). The only new editor duty is to **save the caret `Range` before the modal opens and restore it on Insert**.
  - **Container = modal, not popover** (decided after weighing both): room for the growing search + native-fields + live-metafields content and **zero positioning math** inside the embedded admin iframe (popover anchoring is fiddly there — see [[polaris-web-component-gotchas]]). Popover is a possible later polish, not now.
  - **No active caret → Insert field is disabled** (locked).
  - **Retires the placeholder pill:** `placeholderMetafieldPart()` and the `Metafield · choose field` state are gone — pills are only ever rendered complete, so Step 4 has no empty-pill state to style/guard and no incomplete reference can reach the storefront. The `Metafield · <key>` / `Field · <field>` labels + icons stand. **`data-model.md` needs no change** (METAFIELD already requires namespace + key — the model is now strictly honored, with no transient empty-key state).
  - **Shared reducer action:** insertion uses the **same caret-aware insert/split** action that 4.4's `LINE_BREAK` needs — one action serves both pill insert and line break.
  - **Step 5 redefined** from "popover anchored to a placeholder pill" to "the **Insert field** modal shell + caret save/restore + caret-aware insert"; Steps 6–9 are unchanged in substance (they populate the modal). See the pick-then-insert amendment in `context/features/05-editor-step4-inline-pills.md`.
  - **Editing an existing pill (confirmed 2026-06-15):** click a pill → reopen the modal **pre-filled** → **Update** replaces the field (Cancel leaves it untouched). One modal serves both create (**Insert**) and edit (**Update**).

- **Editor Step 4 build notes (Session 2026-06-16):** Built and browser-verified the contenteditable value surface (see the "Editor Step 4 complete" entry under Completed). Two in-browser findings forced deviations from the Step 4 doc's assumptions, plus one piece of verification scaffolding:
  - **Polaris `s-*` color tokens are NOT exposed to light-DOM CSS (confirmed in-browser).** `--p-color-*` / `--s-color-*` all resolve empty on `document.documentElement`, `body`, and even on `s-*` hosts — they live inside each component's shadow DOM. So the Step 4 doc's plan to style the token via `var(--p-color-text-link)` does not work in this build. **Resolution:** `useCapturedTokenColor()` reads Polaris's own link color once from a throwaway `<s-link>`'s (open) shadow root — measured `rgb(0, 91, 211)` — and publishes it as `--appx-token-color` on the document; the scoped CSS uses that var for the token text and mixes the hover/caret-on tints from it via `color-mix`. This keeps the blue a genuine Polaris value with **no hardcoded hex**, degrading to `currentColor` if the read fails. (New gotcha worth a memory — see [[polaris-web-component-gotchas]].)
  - **Contenteditable trailing-empty-line caret (the two-`<br>` trick).** A single trailing `<br>` does not create an addressable empty line — the browser snaps the caret before it, so pressing Enter at end-of-content and typing put the new text on the *previous* line. Fix: render empty TEXT runs as real (interior) empty text nodes for caret targets, and `syncTrailingFiller` appends a trailing **filler `<br data-filler>`** exactly when the last line is empty (removed the moment content is typed, so it never paints a phantom line). Every DOM walk in `valueDom.ts` skips the filler; it is never part of `valueParts`. This was the step's main risk (the Open question in the feature doc) and is now settled.
  - **Verification scaffolding (dev-only):** because there is no Save path yet, every template loads with `rows: []`, so `SpecTableEditor` seeds a finished sample (`devSampleRows`: a `METAFIELD` token inside "Up to … hours" + a two-line "Features" value) **only when `import.meta.env.DEV`** and rows is empty, with fixed ids (no hydration mismatch). This is how Step 4's token/caret/multiline behavior is exercised without the Step 5 picker. **(Update: kept past Step 5 — there is still no Save/load path, so without it the editor has nothing to edit. It is removed when persistence lands, not at Step 5.)**

- **Editor Step 5 build notes (Session 2026-06-16):** Built and browser-verified the "Insert field" modal + caret bridge (see the "Editor Step 5 complete" entry under Completed). The Step 5 doc's two open questions are resolved, with one deliberate design deviation worth recording:
  - **`<s-modal>` works in this CDN build — control it with the App Bridge Modal API.** The doc flagged a build-risk (this build has a history of web components not matching docs). In-browser, `<s-modal>` opens/closes reliably via `shopify.modal.show(id)` / `shopify.modal.hide(id)` (from `useAppBridge()`), renders the `primary-action` / `secondary-actions` slotted buttons, and provides the focus trap, Esc, and outside-click dismiss **natively**. The declarative `commandFor`/`command` path was **not** needed and is not used. No hand-built focus trap, no positioning math — the reason a modal beat a popover.
  - **Saved-selection model (deviation from the doc's literal gate).** The doc proposed "value-cell blur nulls the live caret + disables the button," guarded against the click-swallow race by `preventDefault()` on the button's `pointerdown`. In practice that is fragile on an `<s-button>` web component **and** breaks keyboard use (you must move focus off the cell to reach the button). **Resolution:** a value-cell blur does **not** clear `activeCaretRef` or disable the button; the gate is dropped only when a **Label / Section field gains focus** (`onFocus` → `onCaretChange(rowId, null)`), and `onDelete` clears it if the saved row is removed. This is the standard rich-text-toolbar "saved selection" pattern: the toolbar operates on the last value-cell caret even though focus has moved to the button/modal. It eliminates the disabled-flip-swallows-the-click race **and** keeps the button keyboard-reachable. Consequence: a pure blur to empty space leaves the button enabled with a saved caret (intended) — the gate tracks "are you editing a value," not "is a value cell literally focused right now."
  - **One caret-restore path, reused.** An external (modal) insert restores the caret through the **same** `ValueCell` reconcile-effect mechanism as an internal edit — the container queues `saved.linear + 1` in a ref-held `pendingCaretByRowRef` Map (stable identity, memoization-safe), and the effect focuses the host + `setCaretLinear` when it sees an entry for its row. No second caret system.
  - **Step 5 stub pill (`STEP5_STUB_PILL`).** Insert drops a fixed, *complete* `SHOPIFY_FIELD` `vendor` pill — honoring the always-complete invariant (never the retired keyless placeholder) — purely to prove the wiring. **Step 6 replaces it** with the field the merchant picks and turns Insert into disabled-until-a-field-is-selected.

- **Editor Step 6 build notes (Session 2026-06-17):** Built and browser-verified the native-field list + one-modal-two-modes (create/Insert + edit/Update). See the "Editor Step 6 complete" entry under Completed. The Step 6 doc's two open questions are resolved:
  - **`<s-choice-list>` + `<s-choice>` WORK in this CDN build** (a positive, given the [[polaris-web-component-gotchas]] track record) — they render a keyboard-navigable single-select radio list, a controlled `values` prop reflects the pre-fill (edit mode opens with the pill's field already selected), and `onChange` reports the chosen value. No fallback to a custom `role="option"` listbox was needed. One type note: `variant` is **not** a prop on the React wrapper (`ReactProps` only `Pick`s `details|disabled|error|label|labelAccessibilityVisibility|multiple|name|values`), so it is omitted — the default already renders as a vertical list. Read the choice via `(event.currentTarget as { values?: string[] }).values[0]`.
  - **Editing a `METAFIELD` pill (accepted, not special-cased):** with only native fields in the list, clicking a `METAFIELD` token (only present via `devSampleRows`) opens the modal **unselected**, so Update stays disabled until a native field is picked — and picking one **converts** the token to a `SHOPIFY_FIELD` (via `SET_VALUE_PART`). This is fine for Step 6; real metafield editing arrives in Step 9.
  - **Stable `onEditPart` keeps row memoization intact:** the clicked `ValueCell` resolves the token's part index (`partIndexOfElement`) and passes the prefill field up, so the container's edit handler needs **no `rows` dependency** (deps `[shopify]`) and stays referentially stable across edits — every non-edited row still skips re-rendering.
  - **One caret-restore path, both modes:** create queues `saved.linear + 1` and edit queues `partOffsetToLinear(valueParts, partIndex + 1, 0)` into the same `pendingCaretByRowRef` Map the Step 5 reconcile effect already consumes — no second caret system. `SET_VALUE_PART` is a length-preserving swap, so the edit caret math runs against the current (pre-swap) `valueParts`.

- **Editor Step 7 build notes (Session 2026-06-17):** Built and browser-verified the modal search box (see the "Editor Step 7 complete" entry under Completed). The Step 7 doc's three open questions are resolved, with one in-browser deviation worth recording:
  - **Auto-focusing the search field must be DEFERRED past the modal's open animation (new gotcha).** App Bridge's `<s-modal>` plays a **view transition** when it shows. The first implementation focused the search field via a double-`requestAnimationFrame` (~32 ms after `shopify.modal.show`), i.e. **mid-transition** — this aborted the view transition and surfaced **`InvalidStateError: Transition was aborted because of invalid state`** in the **admin's** console (logged on the top admin page, not our bundle), and once correlated with a modal that wouldn't close on Insert. Step 6 had *no* console errors, so the regression was clearly the new focus call. **Resolution:** focus the field with a `setTimeout(…, 350)` so it lands **after** the open animation. Verified in-browser that this both (a) focuses the search box on open (the merchant can type immediately — typing "weight" with no prior click filtered the list) **and** (b) produces **no** `InvalidStateError` across repeated open/close/insert/edit cycles. (Removing auto-focus entirely also cleared the error but left native focus on the modal's ✕ close button, so the merchant had to click/Tab to the search box first — the deferred focus keeps the type-immediately UX without the error.) This answers the doc's "Auto-focus inside `<s-modal>`" open question: immediate focus fights the transition; deferred focus does not. Worth a memory — see [[polaris-web-component-gotchas]].
  - **`<s-search-field>` works in this CDN build** (another positive vs the [[polaris-web-component-gotchas]] track record): labelled (`labelAccessibilityVisibility="exclusive"` hides it visually, keeps it for SR), controllable via `value`, and `onInput` fires per keystroke (before `onChange`) with the text on `event.currentTarget.value`. It also renders a native clear (✕) affordance. No fallback to a plain `<input type="search">` was needed.
  - **`<s-choice-list>` survives its `<s-choice>` children being filtered** — the controlled `values={[selectedField]}` keeps reflecting/committing the selection even when that value is **not currently rendered** (the keep-selection round-trip: select → filter out → still selected → commit works). This validated the doc's locked "search is find-only, never deselects" decision in the actual build.
  - **Selection vs. filtering is independent** (locked decision, confirmed live): `searchQuery` and `selectedField` never touch each other; the committed pill is whatever `selectedField` holds regardless of what is visible. Reset hygiene holds — `searchQuery`/`selectedField`/`editTarget` all reset on every open and on Cancel, so create/edit opens carry no stale query, pick, or target.
